name: 净值数据自动抓取

on:
  
  schedule:
    - cron: '15 13 * * *'
  
  # 通过手动触发
  workflow_dispatch:
  
  # 在推送到主分支时触发，且仅当指定的文件发生变化时
  push:
    branches:
      - main
    paths:
      - '.github/workflows/fund_spider.yml'
      - 'fund_spider.py'
      - 'C类.txt'

jobs:
  run_spider:
    # 使用最新的 Ubuntu 操作系统环境
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码 (Checkout repository)
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        
    - name: 设置时区 
      run: |
        sudo apt-get update
        sudo apt-get install -y tzdata 
        sudo timedatectl set-timezone 'Asia/Shanghai'
        
    - name: 安装 Python 依赖
      run: |
        # 安装 fund_spider.py 所需的所有库
        pip install pandas requests aiohttp beautifulsoup4 tenacity json5 jsbeautifier lxml
        
    - name: 运行基金爬虫脚本
      run: |
        echo "开始执行 fund_spider.py..."
        # 运行爬虫脚本，抓取并更新数据
        python fund_spider.py
        
    - name: 检查并提交更改 (Commit and Push changes)
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "🤖 自动更新：抓取最新的基金净值数据"
        # 自动添加 fund_data 目录下的所有 CSV 文件，以及 C类.txt 
        file_pattern: 'fund_data/*.csv C类.txt'
        commit_options: '--allow-empty'
