name: 💰 Parallel Fund Data Collection & Push

# 触发工作流的事件
on:
  # 1. 自动触发：当 main 分支有代码推送时
  push:
    branches:
      - main  # 请根据您的默认分支名称修改
    paths:
      - 'fund_data_collector_parallel.py' # 仅当核心脚本变动时触发
      - 'C类.txt'                        # 仅当基金列表变动时触发
      - '.github/workflows/fund_collector_parallel_workflow.yml'
  
  # 2. 手动触发
  workflow_dispatch:
    
  # 3. 定时触发：每天上海时间早上 10:00 运行 (UTC 02:00)
  schedule:
    - cron: '0 2 * * *' 

jobs:
  collect_and_push:
    runs-on: ubuntu-latest
    
    env:
      # 设置时区为上海，确保日志时间戳正确
      TZ: Asia/Shanghai

    steps:
      - name: ⬇️ 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' 

      - name: ⚙️ 安装依赖
        run: |
          python -m pip install --upgrade pip
          # 确保安装 requests 和 pandas
          pip install requests pandas

      - name: 🏃 运行并行基金数据收集脚本
        # 注意：这里运行的是新的并行脚本名称
        run: |
          python fund_data_collector_parallel.py
          
      - name: 🔄 推送生成的数据到仓库
        id: push_data
        run: |
          # 检查是否有新的 fund_data 目录或文件生成
          if [ -d "fund_data" ] && [ "$(ls -A fund_data | wc -l)" -gt 0 ]; then
            
            echo "--- 配置 Git ---"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            
            echo "--- 添加并提交文件 ---"
            git add fund_data
            
            # 检查是否有实际的更改需要提交 (只提交 fund_data 文件夹下的更改)
            if ! git diff --cached --exit-code --quiet; then
                
                COMMIT_MESSAGE="自动更新：[Data-Parallel] 基金数据更新于 $(date +'%Y-%m-%d %H:%M:%S')"
                
                git commit -m "$COMMIT_MESSAGE"
                
                echo "--- 推送更改 ---"
                git push
                echo "status=pushed" >> $GITHUB_OUTPUT
            else
                echo "无新的数据变化，无需提交。"
                echo "status=no_change" >> $GITHUB_OUTPUT
            fi
          else
            echo "未找到 fund_data 目录或其中没有文件，跳过推送。"
            echo "status=no_data" >> $GITHUB_OUTPUT
          fi
