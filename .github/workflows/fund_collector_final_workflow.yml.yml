name: 💰 Final Fund Data Collection & Push (Parallel + Fixed)

# 触发工作流的事件
on:
  # 1. 自动触发：当 main 分支有代码推送时
  push:
    branches:
      - main  # 请根据您的默认分支名称修改
    paths:
      - 'fund_data_collector_final.py' 
      - 'C类.txt'                      
      - '.github/workflows/fund_collector_final_workflow.yml'
  
  # 2. 手动触发
  workflow_dispatch:
    
  # 3. 定时触发：每天上海时间早上 10:00 运行 (UTC 02:00)
  schedule:
    - cron: '0 2 * * *'  # UTC 02:00 等于 上海时间 (Asia/Shanghai) 10:00

jobs:
  collect_and_push:
    runs-on: ubuntu-latest
    
    env:
      # 设置时区为上海，确保日志时间戳正确
      TZ: Asia/Shanghai

    steps:
      - name: ⬇️ 检出代码
        uses: actions/checkout@v4
        # 必须设置 fetch-depth: 0 以便后续步骤能够推送更改
        with:
          fetch-depth: 0  
          token: ${{ secrets.GITHUB_TOKEN }} # 使用内置 token 授权推送

      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'  

      - name: ⚙️ 安装依赖 (requests, pandas, beautifulsoup4)
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas beautifulsoup4 # <-- 新增 beautifulsoup4

      - name: 🏃 运行修复后的并行数据收集脚本
        run: |
          python fund_data_collector_final.py
          
      - name: 🔄 推送生成的数据到仓库
        id: push_data
        run: |
          # 动态获取当前的年月目录名，例如 202511
          YEAR_MONTH_DIR=$(date +'%Y%m')
          
          # 检查是否有新的 年月 目录生成，并且目录不为空
          if [ -d "$YEAR_MONTH_DIR" ] && [ "$(ls -A "$YEAR_MONTH_DIR" | wc -l)" -gt 0 ]; then
            
            echo "--- 配置 Git ---"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            
            echo "--- 添加并提交文件 ---"
            git add "$YEAR_MONTH_DIR"
            
            # 检查是否有实际的更改需要提交 
            if ! git diff --cached --exit-code --quiet; then
                
                COMMIT_MESSAGE="自动更新：[Data-Fixed-Final] 基金数据更新于 $(date +'%Y-%m-%d %H:%M:%S')"
                
                git commit -m "$COMMIT_MESSAGE"
                
                # 解决推送失败的关键步骤
                echo "--- 拉取远程更改并合并 (git pull --rebase) ---"
                git pull --rebase
                
                echo "--- 推送更改 ---"
                git push
                echo "status=pushed" >> $GITHUB_OUTPUT
            else
                echo "无新的数据变化，无需提交。"
                echo "status=no_change" >> $GITHUB_OUTPUT
            fi
          else
            echo "未找到 $YEAR_MONTH_DIR 目录或其中没有文件，跳过推送。"
            echo "status=no_data" >> $GITHUB_OUTPUT
          fi
