name: 规模
# 触发条件
on:
  # 1. 允许手动触发 (Workflow Dispatch)
  workflow_dispatch:
    inputs:
      reason:
        description: '手动运行的原因'
        required: false
        default: 'Manual run'

  # 2. 当脚本或配置发生变动时自动运行
  push:
    branches:
      - main  # 假设你的主分支是 main
    paths:
      - 'scrape_fund_data.py'  # 脚本文件
      - 'C类.txt'              # 基金代码输入文件
      - '.github/workflows/crawl.yml' # YML 自身变动

# 任务列表
jobs:
  scrape_and_commit:
    runs-on: ubuntu-latest
    
    # 设置时区为亚洲/上海，影响脚本内部的时间函数 get_shanghai_time() 
    env:
      TZ: 'Asia/Shanghai'

    steps:
      - name: 📥 检出代码 (Checkout Repository)
        uses: actions/checkout@v4
        with:
          # 必须设置 fetch-depth: 0 才能在后续步骤中进行完整的历史提交和推送
          fetch-depth: 0 
          
      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' 
          
      - name: ✨ 安装依赖库
        # 脚本中使用了 requests, bs4, pandas
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pandas
          
      - name: 🚀 执行基金数据抓取脚本
        run: |
          python scrape_fund_data.py
          
      - name: ⚙️ 配置 Git 用户信息
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 🔍 检查是否有新文件生成或变动
        id: git_status
        run: |
          # 修正: 使用 awk '{print $2}' 提取 Git 状态输出中的第二列，即文件路径
          NEW_FILES=$(git status --porcelain | grep '^\(A\|M\)' | grep -E '\d{4}/\d{2}/总表_.*\.csv$' | awk '{print $2}' | xargs)
          
          if [ -n "$NEW_FILES" ]; then
            echo "files_changed=true" >> $GITHUB_OUTPUT
            echo "new_files=$NEW_FILES" >> $GITHUB_OUTPUT
          else
            echo "files_changed=false" >> $GITHUB_OUTPUT
          fi
        shell: bash
        
      - name: ➕ 暂存并提交抓取结果
        # 使用新输出变量的访问语法: steps.git_status.outputs.<variable_name>
        if: steps.git_status.outputs.files_changed == 'true'
        run: |
          # 暂存所有新生成的或修改的 .csv 文件和历史数据相关的变动
          git add ${{ steps.git_status.outputs.new_files }}
          git add C类.txt # 确保输入文件变动也被提交
          
          # 创建提交信息
          COMMIT_MSG="🤖 Auto Scrape: Update fund data on $(date '+%Y-%m-%d %H:%M:%S')"
          
          echo "Commiting files: ${{ steps.git_status.outputs.new_files }}"
          git commit -m "$COMMIT_MSG"
          
      - name: 📤 推送新生成的或变动的文件到仓库
        if: steps.git_status.outputs.files_changed == 'true'
        run: |
          git push
          echo "✅ Successfully pushed new data to repository."
