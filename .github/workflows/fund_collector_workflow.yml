name: 💰 Fund Data Collection & Push

# 触发工作流的事件
on:
  # 1. 自动触发：当 main 分支（或您的默认分支）有代码推送时
  push:
    branches:
      - main  # 请根据您的默认分支名称修改
    paths:
      - 'fund_data_collector.py' # 仅当核心脚本变动时触发
      - 'C类.txt'              # 仅当基金列表变动时触发
      - '.github/workflows/fund_collector_workflow.yml' # 仅当工作流变动时触发
  
  # 2. 手动触发：允许在 GitHub Actions 界面手动运行
  workflow_dispatch:
    
  # 3. 定时触发：每天上海时间早上 10:00 运行
  # 上海时间 (CST) 是 UTC+8。因此，上海时间 10:00 对应 UTC 02:00。
  # cron 表达式: m h d m w (分钟 小时 日期 月份 星期)
  schedule:
    # 每天 UTC 02:00 运行 (上海时间 10:00)
    - cron: '0 2 * * *' 

jobs:
  collect_and_push:
    runs-on: ubuntu-latest
    
    # 设置工作流的默认时区为上海，确保日志中的时间是正确的，但 cron 触发器仍然是 UTC
    env:
      TZ: Asia/Shanghai

    steps:
      - name: ⬇️ 检出代码
        uses: actions/checkout@v4
        # 必须设置 token 和 fetch-depth 以便后续步骤能够推送更改
        with:
          fetch-depth: 0 
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' 

      - name: ⚙️ 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas

      - name: 🏃 运行基金数据收集脚本
        run: |
          python fund_data_collector.py
          
      - name: 🔄 推送生成的数据到仓库
        id: push_data
        run: |
          # 检查是否有新的 fund_data 目录或文件生成
          if [ -d "fund_data" ] && [ "$(ls -A fund_data | wc -l)" -gt 0 ]; then
            
            echo "--- 配置 Git ---"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            
            echo "--- 添加并提交文件 ---"
            # 将 fund_data 文件夹下的所有文件添加到 Git 暂存区
            git add fund_data
            
            # 检查是否有实际的更改需要提交
            if ! git diff --cached --exit-code --quiet; then
                # 获取最新的数据文件名（用于提交信息）
                LATEST_FILE=$(ls -t fund_data/fund_data_*.csv | head -1)
                COMMIT_MESSAGE="自动更新：[Data] 基金数据更新于 $(date +'%Y-%m-%d %H:%M:%S')"
                
                git commit -m "$COMMIT_MESSAGE"
                
                echo "--- 推送更改 ---"
                # 推送更改到当前分支
                git push
                echo "status=pushed" >> $GITHUB_OUTPUT
                echo "Commit message: $COMMIT_MESSAGE"
            else
                echo "无新的数据变化，无需提交。"
                echo "status=no_change" >> $GITHUB_OUTPUT
            fi
          else
            echo "未找到 fund_data 目录或其中没有文件，跳过推送。"
            echo "status=no_data" >> $GITHUB_OUTPUT
          fi
