name: Fund Sell Decision Workflow-4

on:
  push:
    paths:
      - 'sell_decision.py'
      - 'holdings_config.yaml'
      - '.github/workflows/fund_decision.yml'
  schedule:
    - cron: '0 0 * * *'  # 优化：每日0点运行（上海时间约8点，净值更新后）
  workflow_dispatch:  # 手动触发

jobs:
  analyze_fund_decisions:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas numpy tenacity tabulate lxml pyyaml
          pip install pandas_ta

      - name: Run sell decision script
        run: |
          python sell_decision.py || echo "Script failed, check logs for details" >&2
        continue-on-error: true  # 脚本失败不中止，记录错误

      - name: Rename CSV results with timestamp 💾
        run: |
          TIMESTAMP=$(date -u +'%Y%m%d_%H%M%S')
          # 检查并重命名新的 CSV 输出文件
          if [ -f sell_decision_results.csv ]; then
            mv sell_decision_results.csv sell_decision_results_${TIMESTAMP}.csv
            echo "Successfully archived CSV file as sell_decision_results_${TIMESTAMP}.csv"
          else
            echo "Results CSV file (sell_decision_results.csv) not found, skipping rename." >&2
            # 保持 exit 1 停止流程，如果脚本成功但文件丢失，则认为是致命错误
            exit 1
          fi

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 只添加 timestamped CSV 文件进行归档
          git add sell_decision_results_*.csv
          # 确保 holdings_config.json 即使没有更改，也保留在 add 列表中 (可选，取决于您是否希望每次都提交它)
          # git add holdings_config.json 
          git commit -m "Update sell_decision report with timestamp [$(date -u +'%Y-%m-%d %H:%M:%S UTC')]" || echo "No changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
